# FPGA-UART-Register-Interface
A UART-based communication system written in Verilog/SystemVerilog.  The design receives commands through UART, stores data into an internal register file, and returns register contents through UART read operations.
